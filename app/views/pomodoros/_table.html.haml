%table.table.table-hover
  %thead
    %tr
      %th
      %th Started at
      %th Finished at
      %th Duration
      %th Project
  %tbody.pomodoros
    - pomodoros.each do |pomodoro|
      = render pomodoro
