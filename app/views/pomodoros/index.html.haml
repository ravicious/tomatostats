- content_for :sidebar do
  = render 'projects/sidebar'
  = render 'stats_sidebar'

%h1 Your pomodoros

= form_tag destroy_multiple_pomodoros_path, method: :delete do
  = submit_tag "Delete selected", class: 'btn'
  = render 'table'
